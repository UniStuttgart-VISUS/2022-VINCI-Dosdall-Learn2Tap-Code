import { createContext } from "react";

export const idContext = createContext(null);
export const symbolsContext = createContext(null);
export const combinationContext = createContext(null);
export const modusContext = createContext(null);
export const unitNameContext = createContext(null);
